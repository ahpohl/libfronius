@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Ensure the PkgConfig::LIBMODBUS imported target exists for consumers
find_package(PkgConfig REQUIRED)
pkg_check_modules(LIBMODBUS REQUIRED IMPORTED_TARGET libmodbus)

# Now load the exported targets which reference PkgConfig::LIBMODBUS
include("${CMAKE_CURRENT_LIST_DIR}/froniusTargets.cmake")