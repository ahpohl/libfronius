@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Ensure the PkgConfig::MODBUS imported target exists for consumers
find_package(PkgConfig REQUIRED)
pkg_check_modules(MODBUS REQUIRED IMPORTED_TARGET libmodbus)

# Now load the exported targets which reference PkgConfig::MODBUS
include("${CMAKE_CURRENT_LIST_DIR}/froniusTargets.cmake")